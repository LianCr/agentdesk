# AgentDesk server-side environment. Copy to .env and fill in.
# .env is git-ignored; never commit real values anywhere.

# Supabase project URL (Settings -> API)
SUPABASE_URL=

# Supabase SECRET key (sb_secret_*). Server-side only: never expose to a
# browser, client bundle, log or error message.
SUPABASE_SECRET_KEY=

# Session-pooler Postgres connection string (includes the database
# password). Used only by `npm run db:push` to apply migrations.
SUPABASE_DB_URL=

# OpenAI API key. Used from M2-C on for text-embedding-3-large.
OPENAI_API_KEY=
