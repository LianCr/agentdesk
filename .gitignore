# Dependencies
node_modules/

# macOS
.DS_Store

# Environment (secrets stay local; the placeholder template is tracked)
.env
.env.*
!.env.example

# Editor
.vscode/
.idea/

# Third-party public PDFs are downloaded locally, never committed
data/public-documents/*.pdf

# Local scratch output (visual QA screenshots etc.)
tmp/

# Next.js build output and synced public PDFs (regenerated by predev/prebuild)
.next/
public/documents/
tsconfig.tsbuildinfo
